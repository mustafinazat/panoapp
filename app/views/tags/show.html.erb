<% set_meta_tags title: @tag.name, description: "Найденное по тегу #"+ @tag.name,
                 reverse: true
%>

<% breadcrumb :tag, @tag %>
<h3>Найденное по тегу #<%= @tag.name %></h3>
                <div class="row">
                <div class="col-md-8 col-lg-9">
                    <div class="tz-gallery">
                        <div class="row" id="posts">
   <%= render @tag.posts.opened %>
                        </div>
                         <div class="row" id="virtualtours">
    <%= render @tag.virtualtours.opened %>
                          </div>
                         <div class="row" id="articles">
      <%= render @tag.articles %>
                        </div>
                    </div>
                </div>
                <div class="col-md-4 col-lg-3">
                    <p> </p>
                </div>

                </div>